# Payment Microservice (Dockerized)

This repository contains a complete Payment Microservice implemented with FastAPI and packaged for Docker.

## Quickstart

1. Copy `.env.example` to `.env` and fill in secrets.
2. Run:

    docker compose up --build

3. Visit http://localhost:8000/docs
